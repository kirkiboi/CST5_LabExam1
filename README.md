create a database named login_system, or create a database with a different name just makae sure that the code is adjusted accordingly to select the correct database. 
make sure there are two different users for role-base authentication.
adjust db.php file if ever your host, user, or pass is different.
